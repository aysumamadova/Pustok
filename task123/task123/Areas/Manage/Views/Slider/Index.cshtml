@model List<Slider>
@{
    ViewData["Title"] = "Index";
    int order = 0;
}

<div class="container-fluid">
    <div class="container-fluid d-flex justify-content-between align-items-center">
        <h1>Authors:</h1>
        <a asp-action="create" asp-controller="slider" class="btn btn-primary">Create</a>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">No</th>
                <th scope="col">Title1</th>
                <th scope="col">Title2</th>
                <th scope="col">Subtitle</th>
                <th scope="col">Order</th>
                <th scope="col">Image</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var slider in Model)
            {
                order++;
                <tr>
                    <th scope="row">@order</th>
                    <td>@slider.Title1</td>
                    <td>@slider.Title2</td>
                    <td>@slider.Desc</td>
                    <td>@slider.Order</td>
                    <td>@slider.Image</td>
                    <td>
                        <form method="post">
                            <a asp-action="edit" asp-controller="slider" asp-route-id="@slider.Id" class="btn btn-warning">Edit</a>
                            <button type="submit" asp-action="delete" asp-controller="slider" asp-route-id="@slider.Id" class="btn btn-danger btn-sweet-delete">Delete</button>
                        </form>
                        
                    </td>
                </tr>
            }

        </tbody>
    </table>
</div>

